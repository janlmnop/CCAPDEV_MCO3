module.exports = {
	MONGO_URI: "change to personal URI"
};

// connect using drivers and use the NON-SRV string.
// there should be one question mark in the entire link.
// add the db name after the slash and before the question mark (mongodb.net:27017/reservation_db?)
